<% if defined?(image) && image %>
  <%= render 'spree/shared/image',alt: image_slider.title, image: image, size: :product, itemprop: "image" %>
<% else %>
  <%= render 'spree/shared/image',alt: image_slider.title, image: @product.display_image, size: :product, itemprop: "image" %>
<% end %>
